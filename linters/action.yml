name: SpinCar Linter
author: SpinCar LLC
description: Run a variety of linters and annotate a PR
inputs:
  linter:
    description: Which linter to run
    required: true
    default: ""

runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon: "terminal"
  color: "white"
